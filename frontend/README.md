# bulk-mailer — frontend

React SPA that provides the UI for the bulk-mailer tool. It lets you compose and send a single email, run a bulk send campaign to a list of recipients with `{{placeholder}}` personalization, and configure the SMTP connection — all from the browser without touching any config files directly.